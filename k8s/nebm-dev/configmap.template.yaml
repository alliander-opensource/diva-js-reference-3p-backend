apiVersion: v1
kind: ConfigMap
metadata:
  name: {appName}
  namespace: {namespace}
  labels:
    app: {appName}
data:
  REDIS_HOST: "diva-redis-redis"
  REDIS_PORT: "6379"
  USE_REDIS: "true"
  BASE_URL: "https://dev-diva-js-reference-3p.appx.cloud"
